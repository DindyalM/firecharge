CREATE TABLE User (
    User_Id int NOT NULL AUTO_INCREMENT,
    Username varchar(255),
    Email varchar(255),
    Password varchar(255),
    PRIMARY KEY (User_Id)
); 

// TODO: Habit Table

CREATE TABLE Habit (
    Habit_Id NOT NULL AUTO_INCREMENT,
    User_Id NOT NULL,
    Name varchar(255),
    PRIMARY KEY (Habit_Id),
    FOREIGN KEY (User_Id)
);